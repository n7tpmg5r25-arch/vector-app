# Brand Migration Punch List — Shorepine Government Relations → Shorepine Civic Tech

Brand guide v1.1 reference. Tracks the multi-wave migration after the 2026-04-16 brand pivot.

## P0 + P1 — Core rebrand (shipped 2026-04-16, commit `3f45c6c`)

- Replaced every "Shorepine Government Relations" string with "Shorepine Civic Tech".
- Swapped "Post & Policy" references in `manifest.json`.
- Disclaimers: removed lobbying language; added nonpartisan commitment per §17.
- Home page: "Solo Practice" → "Free & Nonpartisan".
- Methodology: "lobbyist" → "legislative analyst".
- PDF footer: "CONFIDENTIAL" → "vectorwa.org"; "Legislative Intelligence" → "Free Legislative Intelligence".
- ICS calendar PRODID: updated entity name.
- Login page: copyright line updated.
- CLAUDE.md + PORTABILITY.md: internal docs updated.
- Verified: 0 remaining instances of "Shorepine Government Relations" anywhere in repo.

## P2 — Structural changes (shipped 2026-04-16)

### 1. Rename `client_tag` → `tag` in `tracked_bills`

- Supabase migration `brand_p2_rename_client_tag_to_tag` applied via MCP (`ALTER TABLE ... RENAME COLUMN`).
- Verified no views, materialized views, or RLS policies referenced the old column.
- Code references updated: home page select, watchlist select/filter/render, search bulk-add, bill detail insert/update/state, generate-pdf field read, watchlist ICS calendar, weekly-digest query + interface, email-template interface + render.
- React state renamed for consistency: `clientTag` → `tag`, `activeClient` → `activeTag`, `clients` → `tags`, `clientFiltered` → `tagFiltered`, `clientName` → `tagLabel`, `generateClientPDF` → `generateBriefPDF`.
- `docs/PORTABILITY.md` schema section updated.

### 2. Bill detail "Client" label → "Tag"

- `app/app/bill/[id]/page.js`: label, placeholder, and save-button text swapped. Placeholder now: "e.g. Housing, Transit, Education...".

### 3. AI disclosure labels (§14 / §17)

- Bill detail page: added visible `AI-GENERATED` / `AI · EDITED` pill next to the "Plain English Summary" header, with tooltip explaining origin.
- PDF brief: added an explicit `AI-GENERATED SUMMARY` (or `AI-GENERATED · EDITED`) caption line above every summary body; card height recalculated to accommodate it.
- Existing disclaimer footnote ("Generated by a large language model...") retained.

### 4. Meta descriptions (§2 descriptor)

- `app/app/layout.tsx` line 9: "WA Legislative Trajectory Intelligence" → "Free legislative intelligence for Washington State".
- `app/layout.js` line 5: same update (root layout fallback).
- `app/public/sw.js` line 1 comment: updated to the new descriptor.

### 5. Audience copy sweep

- Home, methodology, settings pages scanned — no "client"-as-customer framing remained after P0/P1.
- Disclaimers page correctly retains "does not represent clients before the Legislature" (intentional — it describes what Shorepine Civic Tech does *not* do per §17).
- Notes visibility UI on bill detail: button labels swapped from `internal`/`client` to `private`/`shared`. Underlying DB enum values left intact to avoid a wider data migration; full enum rename deferred (see Deferred section).
- PDF `clientName` → `tagLabel`; "Prepared for" header replaced with "Filtered by tag".

### 6. Title audit (§13 "Founder")

- Grepped for `SCT`, `CEO`, `Owner`, `Principal` across the repo — zero user-facing matches (one false positive in `package-lock.json` integrity hash). No code changes required.

## Verification (P2)

- `grep -E "client_tag|clientTag"` → only the history line in `docs/PORTABILITY.md`.
- `grep -E "activeClient|clientFiltered|generateClientPDF|clientName"` → 0 matches.
- Supabase column rename verified: `tracked_bills` now has `tag` (not `client_tag`).
- `lintable` TypeScript interfaces in `weekly-digest/index.ts` and `_shared/email-template.ts` both use `tag?: string`.

## Deferred

- **`bill_notes.visibility` enum rename** (`'client'` → `'shared'`). UI labels already dropped the word; the enum rename is a separate migration with backfill that can land on its own schedule. Tracking under P2c if/when pursued.
- **Phase 12 client portal** — likely scrapped per brand pivot (see memory).

## Next wave — P2b (signup rework)

The signup/auth flow still says "Contact Shorepine Civic Tech" for unauthorized emails. With the free, nonpartisan positioning, we want open signup (or a clear waitlist) rather than a gated "contact us" flow. Scoped separately.
